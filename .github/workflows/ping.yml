name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -I "https://abtesting-avgz3ka2tmmnzfkrstbevu.streamlit.app/?health=1"